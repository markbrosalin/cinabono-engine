import { LibraryExportToolContract, LibraryImportToolContract } from "./tools.contracts";

export class LibraryExportTool implements LibraryExportToolContract {
    public export(): void {
        /* Реализация на данный момент отсутствует */
    }
}

export class LibraryImportTool implements LibraryImportToolContract {
    public import(): void {
        /* Реализация на данный момент отсутствует */
    }
}
