import type { Graph } from "@antv/x6";
import type { UIEngineContext } from "../../model/types";

export const nodesService = (_graph: Graph, _ctx: UIEngineContext) => {
    return {
        // TODO: createNode, removeNode, findNode, updateNodeData
    };
};
