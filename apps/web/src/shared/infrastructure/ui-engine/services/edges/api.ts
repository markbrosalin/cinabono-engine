import type { Graph } from "@antv/x6";
import type { UIEngineContext } from "../../model/types";

export const edgesService = (_graph: Graph, _ctx: UIEngineContext) => {
    return {
        // TODO: createEdge, removeEdge, updateEdgeSignal, setRouterMode
    };
};
