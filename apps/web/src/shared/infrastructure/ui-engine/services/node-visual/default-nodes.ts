import { LOGIC_VISUAL_PRESETS } from "../../model/nodes-spec/logic";
import type { AnyVisualBinding } from "../../model/visual";

export const defaultVisualPresets: AnyVisualBinding[] = [
    ...LOGIC_VISUAL_PRESETS,
];
