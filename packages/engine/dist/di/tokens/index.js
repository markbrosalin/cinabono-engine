import { CORE_TOKENS, SERVICES_TOKENS, FACTORIES_TOKENS, STORES_TOKENS, } from "../../di/tokens/base";
import { PLUGINS_TOKENS } from "../../di/tokens/plugins";
export const DEPS_TOKENS = {
    core: CORE_TOKENS,
    services: SERVICES_TOKENS,
    factories: FACTORIES_TOKENS,
    stores: STORES_TOKENS,
    plugins: PLUGINS_TOKENS,
};
